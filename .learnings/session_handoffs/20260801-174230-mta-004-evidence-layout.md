# Session handoff: MTA-004 evidence layout

## Handoff metadata

- Created: 2026-08-01T17:42:30+08:00
- Repository root: `/home/young/liufengrong/robot_lab`
- Branch: `main`
- HEAD: `1787e4bc0199ebaa59169fe5d94a5941c8a9dc3d`
- Trigger: the repository threshold of five verified context compactions was reached immediately after the user approved the MTA-004 modification plan.
- Compaction query status: `available`
- Verified compaction count: 5 (exact)
- Threshold: 5
- Thread ID: `019fb726-2ddf-7763-acb3-59988faeb614`
- Compaction evidence time: 2026-08-01T09:41:26.922227+00:00
- Live workspace verification time: 2026-08-01T17:41:57+08:00

## Current objective

Implement MTA-004 for `monitor-tune-isaaclab-training`: provide a deterministic,
safe evidence directory and executable output-path workflow for training health,
training summaries, assessments, criteria, and bounded Play artifacts. Keep raw
evidence separate from immutable tuning-experience events.

The implementation plan described below was explicitly approved by the user in
the message immediately before this handoff. No MTA-004 implementation edit was
started in this session because the fifth verified compaction triggered the
mandatory handoff workflow.

## User decisions and protected constraints

- The user explicitly approved the previously proposed nine-file MTA-004 scope.
- The skill remains human-in-the-loop and advisory-only. Do not start, stop,
  resume, restart, or signal training; do not launch Play unless separately
  requested and permitted.
- Do not edit training parameters or add autonomous tuning, multi-seed,
  MuJoCo, remote Git-mailbox coordination, or deployment automation.
- Do not install or delete anything.
- Do not commit or push as part of MTA-004 unless the user separately requests
  and approves the exact commit scope.
- Protect `rough_env_cfg.py` and `terrains_cfg.py`: do not stage, overwrite,
  revert, or clean them. They are outside the approved MTA-004 scope.
- Preserve all unrelated user changes. If live status differs from this
  handoff or the nine-file scope must expand, stop and present a revised plan
  for explicit approval before editing.

## Completed work

- MTA-002 was committed as `0faa3dd` (`feat(skill): require approved assessment criteria`).
- MTA-003 was committed as `1787e4b` (`feat(eval): expose policy telemetry completeness`).
- MTA-003 validation reported 52 retained tests, Skill validation, 11 algorithm
  profiles, coverage and CLI checks, evaluator help, reference checks,
  `git diff --check`, whitespace checks, and sensitive-information scans passing.
- The current MTA-004 implementation was only inspected and planned. No
  MTA-004 source, documentation, test, or backlog file has been changed.
- The latest read-only inspection confirmed the old backlog wording about a
  relative assessment output is partly stale: current assessment examples use
  abstract absolute `/ABSOLUTE/EVIDENCE/...` placeholders. The remaining issue
  is that those paths are not produced by a deterministic repository-local
  layout; health still relies on shell redirection, and training-summary output
  does not enforce an absolute, non-overwriting destination.

## Live-verified state

Verified at 2026-08-01T17:41:57+08:00:

- Repository root: `/home/young/liufengrong/robot_lab`.
- Branch and upstream: `main...origin/main`, ahead 13 and behind 0.
- HEAD: `1787e4bc0199ebaa59169fe5d94a5941c8a9dc3d`.
- Worktree: clean; `git diff --name-only` returned no paths.
- Index: clean; `git diff --cached --name-only` returned no paths.
- No uncommitted `rough_env_cfg.py` or `terrains_cfg.py` changes were present.
- Existing training PID 490073 was running
  `scripts/reinforcement_learning/rsl_rl/train.py` for
  `RobotLab-Isaac-Velocity-Rough-LW-leg-Amp-Roa-v0`, 4096 environments and
  100000 maximum iterations.
- Existing training PID 510774 was running the same task and major arguments.
- No `play.py` or `evaluate_policy.py` process matched the read-only process scan.
- GPU utilization, current training steps, run directories, log freshness,
  checkpoint state, and external W&B state were not inspected and remain
  unverified because they are not required for the MTA-004 code change.
- No training or evaluation process was started, stopped, or signaled.

## Pending and unverified work

The first bounded action in the new session is to re-run read-only status, HEAD,
process, and compaction checks, then inspect the exact target files for drift.
If the approved scope still matches, implement only these nine files:

1. Add `.agents/skills/monitor-tune-isaaclab-training/scripts/prepare_evidence_layout.py`.
2. Add `.agents/skills/monitor-tune-isaaclab-training/references/evidence-layout.md`.
3. Add `.agents/skills/monitor-tune-isaaclab-training/tests/test_evidence_layout.py`.
4. Modify `.agents/skills/monitor-tune-isaaclab-training/scripts/collect_training_health.py`.
5. Modify `.agents/skills/monitor-tune-isaaclab-training/scripts/summarize_training_log.py`.
6. Modify `.agents/skills/monitor-tune-isaaclab-training/tests/test_training_health.py`.
7. Modify `.agents/skills/monitor-tune-isaaclab-training/SKILL.md`.
8. Modify `.agents/skills/monitor-tune-isaaclab-training/references/human-guided-training-advisor.md`.
9. After all validation passes, update only MTA-004 in `.learnings/FEATURE_REQUESTS.md`.

Approved implementation behavior:

- Use `learnings/policy_tuning/<task>/<run-id>/evidence/` with subdirectories
  for criteria, health, training, and `play/<evaluation-id>/`.
- The layout helper accepts safe task, run, snapshot, and optional evaluation
  identifiers and returns absolute paths for criteria, health, summary,
  assessment, Play result, telemetry, and video artifacts.
- Reject unsafe identifiers, traversal, symlinked path components, and existing
  target evidence. Do not perform Git actions.
- Add optional `--output` to `collect_training_health.py`; require a new
  absolute path for file output while preserving stdout behavior when omitted.
- Require `summarize_training_log.py --output` to be a new safe absolute path.
- Keep existing scripts that already enforce absolute output behavior; update
  documentation to consume helper-produced paths instead of abstract
  `/ABSOLUTE/EVIDENCE` examples.
- Document that raw evidence lives under `evidence/`; immutable timestamped
  experience events remain at the run root via `record_tuning_experience.py`.
  Events reference evidence paths and hashes; referenced evidence must not be
  overwritten, and later observations use new snapshot IDs.

Required validation remains pending:

- Tests for deterministic absolute paths, multiple non-conflicting snapshots
  and evaluations, identifier traversal rejection, symlink rejection,
  non-overwrite behavior, and the evidence/event boundary.
- Tests that health and summary reject relative output and existing targets.
- All retained Skill tests, Skill validator, 11 algorithm-profile checks,
  coverage scan, relevant CLI/help loads, reference checks, `git diff --check`,
  whitespace checks, and sensitive-information scan.
- Review the final diff and status to confirm that only the approved nine files
  plus this required handoff document are changed. Do not stage the handoff or
  any implementation file without separate commit approval.

## Risks and do-not-repeat actions

- Do not treat `/ABSOLUTE/EVIDENCE` examples or the MTA-004 backlog description
  as proof of current behavior; inspect the current files first.
- Do not accidentally write `SUMMARY.json`, health JSON, or temporary files to
  the repository root during tests. Use a test temporary directory.
- Do not weaken current absolute-path and non-overwrite guarantees in
  `assess_training_run.py`, `evaluate_policy.py`, checkpoint selection, export,
  or archive tooling.
- Do not conflate raw evidence files with append-only experience-event JSON.
- Do not overwrite or follow symlinked evidence targets.
- Do not repeat MTA-002 or MTA-003, and do not begin MTA-005.
- Do not start Play or use the two live training processes as test fixtures.
- Do not install dependencies, remove files, operate remote Git, commit, or push.

## Recommended next steps

1. Start a new session with the prompt below.
2. Read `AGENTS.md`, this handoff, and the three directly relevant skills in
   full: `monitor-tune-isaaclab-training`, `skill-creator`, and
   `inspect-context-compactions`. Use `self-improvement` only as required for
   compaction checks or another handoff.
3. Re-verify clean status, HEAD, relevant processes, protected paths, and target
   file drift before editing.
4. Because the exact nine-file plan has already been explicitly approved,
   proceed with it if live scope is unchanged. If any additional file or
   materially different behavior is needed, present the revised plan and wait
   for explicit approval.
5. Implement with `apply_patch`, run the full approved validation set, and
   report results without committing or pushing.

## New-session prompt

```text
请在 /home/young/liufengrong/robot_lab 继续 MTA-004。先完整阅读：
1. /home/young/liufengrong/robot_lab/AGENTS.md
2. /home/young/liufengrong/robot_lab/.learnings/session_handoffs/20260801-174230-mta-004-evidence-layout.md
3. /home/young/liufengrong/robot_lab/.agents/skills/monitor-tune-isaaclab-training/SKILL.md
4. /home/young/.codex/skills/.system/skill-creator/SKILL.md
5. /home/young/liufengrong/robot_lab/.agents/skills/inspect-context-compactions/SKILL.md

开始时只读核对 git status、HEAD、相关训练/评估进程、压缩次数、受保护文件和九个目标文件是否漂移，不要把旧会话状态直接当成当前事实，也不要重复 MTA-002/MTA-003。MTA-004 的九文件修改方案已经由我明确批准；若实时范围与交接一致，可直接按交接中的批准方案实施，无需再次征求相同方案的批准。若需要扩展文件范围或改变行为，遵守 AGENTS.md，先给出修订方案并等待我的明确批准。

保护 rough_env_cfg.py 和 terrains_cfg.py，不得暂存、覆盖、回退或清理；不要启动、停止或干扰训练，不要启动 Play，不要安装或删除内容，不要操作远端 Git 邮箱，不要提交或推送，不要开始 MTA-005。使用 apply_patch 实施，完成交接列出的完整测试、Skill validator、diff、引用、空白和敏感信息检查，验证通过后仅更新 MTA-004 状态并报告结果。
```
