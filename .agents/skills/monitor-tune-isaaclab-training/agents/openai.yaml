interface:
  display_name: "IsaacLab Training Setup & Tuner"
  short_description: "Configure, supervise, and tune IsaacLab policies"
  default_prompt: "Use $monitor-tune-isaaclab-training to configure or verify first-run machine and Git-mailbox prerequisites, then supervise and tune the approved IsaacLab policy workflow."
