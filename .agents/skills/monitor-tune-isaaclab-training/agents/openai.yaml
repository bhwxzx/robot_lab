interface:
  display_name: "IsaacLab Training Setup & Tuner"
  short_description: "Configure, supervise, and safely orchestrate IsaacLab tuning"
  default_prompt: "Use $monitor-tune-isaaclab-training to inspect campaign, policy export, parity, and evaluation handoff in shadow mode, then advance only the explicitly approved bounded IsaacLab workflow."
